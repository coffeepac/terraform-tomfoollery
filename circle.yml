machine:
  pre:
    - curl https://releases.hashicorp.com/terraform/0.6.8/terraform_0.6.8_linux_amd64.zip ~/terraform.zip
    - sudo unzip terraform.zip -d /usr/local/bin/
    - sudo curl https://s3-us-west-1.amazonaws.com/22acacia-deploy/custom-binaries/terraform-provider-googlecli -o /usr/local/bin/terraform-provider-googlecli

deployment:
  shitsnacks:
    branch: master
    commands:
      - terraform remote config -backend=s3 -backend-config="access_key=AKIAIO7EFPBVLETOC4IA" -backend-config="secret_key=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" -backend-config="region=us-west-1" -backend-config="bucket=22acacia-deploy" -backend-config="key=terraform-state/temp2"
      - terraform plan
      - terraform apply
      - terraform remote push
